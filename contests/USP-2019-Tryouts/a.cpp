#include <bits/stdc++.h>
using namespace std;

int n, q, k[50010];
vector<array<int, 50010>> memo;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> n >> q;
	for(int i = 0; i < n; i++)
		cin >> k[i];

	while(q--){
		int l, r, x, m; cin >> l >> r >> x >> m;

		
	}
}